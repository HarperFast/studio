import { MessageCircleIcon } from 'lucide-react';
import { iconSharedClassName } from './constants';

export function HarperAgentIcon() {
	return <MessageCircleIcon className={iconSharedClassName + 'w-4 h-4 shrink-0 text-purple-600'} />;
}
