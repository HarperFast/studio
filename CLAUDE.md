# CLAUDE.md

**[`AGENTS.md`](AGENTS.md) is canonical — read it now.** This file exists only so tools that look
for `CLAUDE.md` by name find their way there; it deliberately holds no notes of its own, so anything
you add here will be missed by every agent that reads `AGENTS.md` instead. New project notes belong
in `AGENTS.md`.

(Machine-specific, personal notes still go in a gitignored `CLAUDE.local.md`, which is not committed.)
